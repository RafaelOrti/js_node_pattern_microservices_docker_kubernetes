# values.yaml para order-service
image:
  repository: order-service
  tag: latest

service:
  name: order-service
  port: 80

env:
  NODE_ENV: production
  DB_HOST: mongodb
  # Agregar más variables de entorno según sea necesario
